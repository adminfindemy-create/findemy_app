# S5.1 — Profile, bookings & account `[F21]` — REUSE (+ small CHANGE)

> Master §P5. Deps: **S2.1** (trial booking), **S3.1** (Classes tab moved trials out). Almost entirely verification — the surfaces exist; the one real change is resolving the orphaned trials screen.

## 1. Feature & intent
The student **Profile** is the home for **bookings, trials, saved, past classes, edit profile**; the academy **Studio** hub holds **profile, settings, KYC/bank**. Trials live under **Profile → Your Bookings**, not the Classes tab (S3.1).

- **Refs:** master §3.10/§4.11; gap-analysis.
- **Post-P4 reality (verified):** student Profile (`(tabs)/profile.tsx`) → **Your Bookings** (`/bookings`) combines trials + workshops + enrolments (`GET /trials/my` + `GET /me/workshop-registrations` + enrolments) — **trials are reachable under Profile** ✅. Saved (`saved.tsx` → `GET /me/saved-academies`), edit-profile, general-info, academy Studio hub (`(tabs)/studio.tsx`), Settings (`(tabs)/settings.tsx` incl. `attendance_reminder` toggle `:81`), bank/KYC (`academy profile/edit.tsx:292-322` → `PUT /studio/academy`) — **all REUSE**. **⚠️ The one gap:** `student-app/app/trials/index.tsx` ("My Trials") is **orphaned** — reachable only via `booking/slot.tsx:86 router.replace('/trials')` after a reschedule; duplicates `/bookings`; not linked from Profile.

## 2. Cross-side impact map
| | Backend | Student app | Academy app |
|---|---|---|---|
| Bookings/trials home | REUSE `GET /trials/my`, `/me/workshop-registrations` | REUSE `/bookings` (trials+workshops+enrolments) | — |
| Orphaned `/trials` **index** | — | **CHANGE** — remove `trials/index.tsx`; repoint **three** refs to `/bookings` (`booking/slot.tsx:86`, `booking/confirmation.tsx:51` `screen:'trials'→'bookings'`, `usePushNotifications.ts:65`). **Keep `trials/[id].tsx`** — it's the live trial-detail used by `BookingDetailSheet:388`. | — |
| Saved / edit profile / general info | REUSE | REUSE | — |
| Studio hub / Settings / KYC | REUSE `GET/PUT /studio/settings`, `PUT /studio/academy` | — | REUSE |

## 3. The shared contract
No new contract — all endpoints exist. The only change is **client routing** (remove `/trials`, repoint the redirect).

## 4. Component design, per side
- **Student app:** **delete only `app/trials/index.tsx`.** **KEEP `app/trials/[id].tsx`** — it's the live trial-detail screen, reached from `BookingDetailSheet.tsx:388` ("Manage trial"). Repoint the **three** refs that pointed at the index → `/bookings`: `booking/slot.tsx:86` (reschedule redirect), `booking/confirmation.tsx:51` (`data:{ screen:'trials' }` → `'bookings'`), `usePushNotifications.ts:65` (`router.push('/trials')`). Confirm Profile's "Your Bookings" is the single trials entry. Add **`trials/index.tsx` only** to the dead-code registry.
- **Academy app:** REUSE Studio hub + Settings + bank/KYC. Verify the attendance-reminder toggle persists (it feeds the S5.2 worker). No build.
- **Backend:** none (REUSE).

## 5. Sequencing & DoD
1. Verify trials-under-Profile end to end. 2. Remove the orphan + repoint the redirect. 3. Verify academy Studio/Settings/KYC round-trip (settings persist).

**Definition of Done (E2E)**
- [ ] A booked trial appears under **Profile → Your Bookings** (with workshops + enrolments), reachable without going through Classes.
- [ ] The orphaned `/trials` screen is **gone** and the reschedule flow lands on `/bookings`; no dead nav refs.
- [ ] Academy edits profile + toggles **attendance reminder** + saves bank details → all persist (`GET /studio/settings`, `PUT /studio/academy`). Typecheck + suite green.

## 6. Handoff
The attendance-reminder toggle is the input to S5.2's worker. The removed `/trials` screen → dead-code registry.
