# S1.2 — Discovery & search `[F3]` — REUSE (verify)

> Feature Master Plan (master-execution-plan §3.2). Deps: S0.4 (seed), S1.1. **Mostly already works** — this slice is verify + small polish, no new backbone.

## 1. Feature & intent
The student finds academies: search, location, filters (min rating, distance), category pills, top-rated carousel, near-you list — all against the **Delhi-NCR** seed. Tap a card → academy detail (S1.1).

- **Refs:** master-feature-document §3.2 (Discover); prototypes Discover + Location/Filters sheets; gap-analysis §2 ("Discovery = works").
- **Post-P0 reality:** `listAcademies` already supports `category`, `min_rating`, `radius_km`, `sort = rating|distance|top_rated`, interests, cursor; Delhi-NCR coords seeded (S0.4) so distance math is sane.

## 2. Cross-side impact map
| | Backend | Academy app | Student app |
|---|---|---|---|
| Search/filter/sort | **REUSE** `academies/service.ts listAcademies` + `routes.ts` (`category`/`min_rating`/`radius_km`/`sort`) | n/a (academy has no discovery — it's *discoverable* via S1.1 profile) | **REUSE** Discover tab + Location/Filters sheets |
| Category pills | REUSE `CategoryEnum` (music/dance/arts/yoga) | n/a | **VERIFY** pills match enum; **`fitness` pending** (see §6) |
| Delhi-NCR copy | done (S0.4 seed + fixtures) | — | VERIFY no stray Bengaluru/Indiranagar strings remain |

## 3. The shared contract
`GET /academies?query&category&min_rating&radius_km&sort&cursor&limit` → `{ items: AcademyCard[], next_cursor }` (REUSE — already in `@findemy/api-client`). No contract change unless `fitness` is adopted (then extend `CategoryEnum`).

## 4. Component design
- **Backend:** REUSE. Confirm ranking (`top_rated` = rating × log(count)) and Haversine distance behave on Delhi coords. If `fitness` adopted: add to prisma `Category` + `CategoryEnum` (+ db push) + seed a fitness academy.
- **Student:** REUSE Discover tab. Verify category pills render from the canonical enum; verify the Location sheet GPS path; confirm "Trial from ₹X" reads `trial_fee_paise`.
- **Academy:** none.

## 5. Sync map
| Item | Backend | Student |
|---|---|---|
| filters (category/rating/distance) | `listAcademies` query | Filters/Location sheets |
| card fields (rating, distance, trial-from) | academy serializer | AcademyCard |

## 6. Open decision (resolve here)
**`fitness` category** — prisma `Category` is `music/dance/arts/yoga` today; master doc mentions fitness. **Decision needed:** add now (enum + db push + a seeded fitness academy + a Discover pill) or defer. **Recommendation: defer** unless the demo needs a fitness academy — it's an additive change that can land any time.

## 7. Sequencing & acceptance
1. Verify backend discovery on the Delhi-NCR seed (curl `/academies?category=music`, `?sort=top_rated`).
2. Verify student Discover pills + filters + near-you reach academy detail.
3. (If `fitness` adopted) extend enum + seed + pill.

**Definition of Done**
- [ ] Student searches + filters and reaches an academy detail; results are Delhi-NCR.
- [ ] Category pills match the canonical enum; no stray Bengaluru copy.
- [ ] `fitness` decision recorded (added or explicitly deferred).

## 8. Handoff outline
Confirm the existing `GET /academies` query contract (params + card shape) is the one both consume; note the `fitness` decision. Minimal — this is a verify slice.
