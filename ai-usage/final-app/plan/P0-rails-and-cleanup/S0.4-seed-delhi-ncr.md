# S0.4 — Seed & demo world (Delhi-NCR) `[F23]` — CHANGE

> Feature Master Plan (master-execution-plan §3.2). One coherent seed both apps demo against. Deps: S0.1. **Re-touched** by S1.3 (discount fields) and S2.3.

## 1. Feature & intent
Replace the Bengaluru seed with the canonical **Delhi-NCR demo world** so both apps line up on the same cast for every later slice's E2E test. The seed *is* the shared fixture referenced by every handoff.

- **Refs:** master §2.1 (shared cast); app-ideation Delhi-NCR decision; gap-analysis (seed is Bengaluru — `backend/api/prisma/seed.ts:272`; two student-app string fallbacks).

## 2. The canonical cast (from master-feature-document §2.1)
- **Featured academy:** **The Rhythm House** — Hauz Khas, Delhi-NCR · ★4.9 · Verified.
- **Coaches:** Arjun Sharma (Guitar·Ukulele), Meera Nair (Vocals·Theory), Rohan Das (Keys·Piano).
- **Batches:** Guitar — Beginner (**in-studio**, ₹150 trial / ₹3,200/mo, 12/15), Vocals — All Levels (**online**, ₹200 / ₹3,800/mo, 8/12), Keys & Piano (**in-studio**, ₹250 / ₹4,200/mo, 6/10).
- **Demo discounts:** quarterly **10%** / annual **20%** *(field added in S1.3 — see §4)*.
- **Other academies (student marketplace):** Step Up Dance Studio (Saket), Clay & Kiln Pottery (Lajpat Nagar), Prana Yoga (Green Park).
- **Student:** **Varun Mehta** · Delhi-NCR · `varun.mehta@gmail.com`.
- **Sample trial OTPs (4-digit):** `4927` (Guitar in-studio) · `8260` (Vocals online).

## 3. Cross-side impact map
| | Backend | Academy app | Student app |
|---|---|---|---|
| Seed | **CHANGE** `prisma/seed.ts` → Delhi-NCR cast above | (reads seeded academy) | (reads seeded marketplace) |
| Hardcoded city strings | — | — | **CHANGE** `app/booking/confirmation.tsx:119` (`"Indiranagar · 1.2 km"`), `app/booking/pay.tsx:247` (`"In studio · Indiranagar"`) → Delhi-NCR (or bind real data) |

## 4. Dependencies & staging (important)
The seed can only set fields that **exist now**. `quarterlyDiscountBps`/`annualDiscountBps` are added in **S1.3**; the 4-digit attendance OTP generation is finalised in **S2.2**. So:
- **S0.4 (now):** locations, academy/coaches/batches (fee + capacity + mode), student, marketplace academies, **drop Bengaluru**. **Keep the attendance OTP 6-digit** (`String(100000+seq)`) to match `generateOtpCode()` + `attendance.test` — the **4-digit** OTP (`4927`/`8260`) is **S2.2**, which changes the generator + seed + test together. The `Slot` model still exists until S2.1, so the seed keeps creating slots (fine — `slots` stays until S2.1).
  - *(Implementation note: an early build set the seed OTP 4-digit; reverted to 6-digit for consistency — see P0 README §"Test-suite reality".)*
- **Re-touch at S1.3:** add `quarterlyDiscountBps=1000` / `annualDiscountBps=2000` to the batches.
- **Re-touch at S2.x:** align trial/enrolment seed rows with the reworked availability + commission.

## 5. Component design
- **Backend:** rewrite `prisma/seed.ts` — idempotent upserts for the cast; Delhi-NCR coordinates (Hauz Khas/Saket/Lajpat Nagar/Green Park) for the discovery distance math; `Category` enum values are `music`/`dance`/`arts`/`yoga` (decide `fitness` per S0.1 open decision); 4-digit OTPs.
- **Student app:** replace the two hardcoded "Indiranagar" fallbacks (low-risk strings).
- **Academy app:** none (city is free-text; no hardcoded Bengaluru — gap-analysis §6).

## 6. Sequencing & acceptance
1. Rewrite seed; `pnpm --filter @findemy/api prisma migrate reset` (or seed) → loads Delhi-NCR.
2. Fix the two student strings.

**Definition of Done**
- [ ] Fresh DB seed shows The Rhythm House (Hauz Khas) + Guitar(in-studio)/Vocals(online)/Keys; student Varun Mehta; marketplace academies in Saket/Lajpat Nagar/Green Park.
- [ ] No "Bengaluru"/"Indiranagar" anywhere in `backend/api` seed or `student-app` strings.
- [ ] Trial OTPs are 4-digit (`4927`/`8260`).
- [ ] Both apps, freshly seeded, show the **same** cast and line up (student sees Guitar/Vocals; academy manages the same batches).

## 7. Handoff outline
The **seed fixtures** section every later handoff references: the seeded IDs/handles for The Rhythm House, the three batches (with mode), Varun Mehta, and the demo OTPs — so both apps develop and demo against identical data.
